name: ExtraLectern
version: '${project.version}'
main: net.sleepyspeller.extralectern.ExtraLectern
api-version: '1.20'
commands:
  reloadConfig:
    description: Reload the config for ExtraLanterns
    usage: /<command>
  resetProgress:
    description: Reset someones progress on ExtraLectern
    usage: /<command>
  setFinalMessage:
    description: Set the final message on ExtraLectern
    usage: /<command>